// The site's animated background. To switch background, point this
// re-export at a different component in this folder — nothing else
// in the app needs to change.
export { default } from './FuturisticHUD'
